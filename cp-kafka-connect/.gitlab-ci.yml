include:
  - local: 'ci-templates/docker-build.yml'

# ======================================
# 🧱 Validate during Merge Request
# ======================================
lint_kafka_connect:
  extends: .lint_dockerfile
  variables:
    SUBDIR: "cp-kafka-connect"
  rules:
    # Run during Merge Requests when pyspark code changes
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main"'
      changes:
        - cp-kafka-connect/Dockerfile
        - cp-kafka-connect/**/*
      when: always
    - when: never

validate_kafka_connect:
  extends: .validate_dockerfile
  variables:
    SUBDIR: "cp-kafka-connect"
  rules:
    # Run during Merge Requests when pyspark code changes
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "main"'
      changes:
        - cp-kafka-connect/Dockerfile
        - cp-kafka-connect/**/*
      when: always
    - when: never

# ======================================
# 🚀 Push after merge to main
# ======================================
build_and_push_kafka_connect:
  extends: .build_and_push_template
  variables:
    SUBDIR: "cp-kafka-connect"
  rules:
    # Only run after merge (push to main)
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
      changes:
        - cp-kafka-connect/Dockerfile
        - cp-kafka-connect/**/*
      when: always
    - when: never
