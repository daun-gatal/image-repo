# ======================================
# Lint Stage
# ======================================
.lint_dockerfile:
  stage: lint
  image: ubuntu:22.04

  before_script:
    - apt-get update -y
    - apt-get install -y wget ca-certificates
    - wget -qO /usr/local/bin/hadolint https://github.com/hadolint/hadolint/releases/latest/download/hadolint-Linux-x86_64
    - chmod +x /usr/local/bin/hadolint

  script:
    - echo "🔍 Running Hadolint on Dockerfile..."
    - hadolint --config "${CI_PROJECT_DIR}/.hadolint.yaml" "${CI_PROJECT_DIR}/${SUBDIR}/Dockerfile"

  tags:
    - k8s
  allow_failure: false

# ======================================
# Validate Stage
# ======================================
.validate_dockerfile:
  stage: validate
  image: docker:27
  services:
    - name: docker:27-dind
  variables:
    DOCKER_HOST: tcp://svc-0:2375
    DOCKER_TLS_CERTDIR: ""
    DOCKER_DRIVER: overlay2
  script:
    - VERSION=$(cat "${CI_PROJECT_DIR}/${SUBDIR}/VERSION")
    - echo "🧪 Validating Dockerfile for version ${VERSION}"
    - docker build --no-cache --build-arg VERSION=${VERSION} -t "dryrun:${CI_COMMIT_SHA}" "${CI_PROJECT_DIR}/${SUBDIR}"
  tags:
    - k8s
  allow_failure: false

# ======================================
# Build & Push Stage
# ======================================
.build_and_push_template:
  stage: build_and_push
  image: docker:27
  services:
    - name: docker:27-dind
  variables:
    DOCKER_HOST: tcp://svc-0:2375
    DOCKER_TLS_CERTDIR: ""
    DOCKER_DRIVER: overlay2
  before_script:
    # Log in to GitLab Container Registry
    - echo "$CI_REGISTRY_PASSWORD" | buildah login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
  script:
    - VERSION=$(cat "${CI_PROJECT_DIR}/${SUBDIR}/VERSION")
    - IMAGE="${CI_REGISTRY_IMAGE}/${SUBDIR}:${VERSION}"
    - echo "🔨 Building ${IMAGE}"
    - docker build --build-arg VERSION="${VERSION}" -t "${IMAGE}" "${CI_PROJECT_DIR}/${SUBDIR}"
    - echo "🚀 Pushing ${IMAGE}"
    - docker push "${IMAGE}"
  tags:
    - k8s
